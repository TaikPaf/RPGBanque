{% extends 'base.html.twig' %}

{% block body %}
    <h1>CompteCourant</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteCourant.id }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ compteCourant.solde }}</td>
            </tr>
            <tr>
                <th>Numerocompte</th>
                <td>{{ compteCourant.numeroCompte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('comptecourant_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('comptecourant_edit', { 'id': compteCourant.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
