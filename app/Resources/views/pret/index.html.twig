{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pret list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Somme</th>
                <th>Interet</th>
                <th>Datecreation</th>
                <th>Datefin</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pret in prets %}
            <tr>
                <td><a href="{{ path('pret_show', { 'id': pret.id }) }}">{{ pret.id }}</a></td>
                <td>{{ pret.somme }}</td>
                <td>{{ pret.interet }}</td>
                <td>{% if pret.dateCreation %}{{ pret.dateCreation|date('Y-m-d') }}{% endif %}</td>
                <td>{% if pret.dateFin %}{{ pret.dateFin|date('Y-m-d') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pret_show', { 'id': pret.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pret_edit', { 'id': pret.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pret_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
